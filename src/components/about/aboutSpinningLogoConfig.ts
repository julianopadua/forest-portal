/**
 * Cycling words shown next to the spinning logo on the marketing landing page.
 *
 * Edit this list to add, remove, or reorder words. Order is preserved at runtime.
 * Keep entries short (~3-12 chars) so the layout stays balanced — the longest entry
 * reserves the line width to avoid horizontal jumps during the typewriter cycle.
 */
export const SPINNING_LOGO_CYCLING_WORDS: string[] = [
  "FOREST",
  "FINANCE",
  "FIRE",
  "FUTURE",
  "FAUNA",
  "FLORA",
  "FORECAST",
  "OPEN SOURCE",
  "OPEN DATA",
  "CLIMATE",
];

export const SPINNING_LOGO_STATIC_WORD = "INSTITUTO";
